import React from "react";

const LoadingText = () => {
    return (
        <div className="w-full rounded-lg bg-gradient-to-r from-gray-400 via-gray-300 to-gray-400 bg-[length:200%_100%] animate-pulseGray"></div>
    );
};

export default LoadingText;